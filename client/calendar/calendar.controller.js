( function () {
	'use strict';

	function CalendarCtrl () {
		// NOTE: commenting next line so that it won't be checked by eslint
		// var self = this;

		// NOTE: this controller is technically empty. 
		// I'm just preparing this just in case there will be addtional calendar events required.
	}

	angular.module( 'app.home' )
		.controller( 'CalendarCtrl', CalendarCtrl );
} )();